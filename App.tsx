import React from 'react';
import Loading from './screens/Loading';
import AppContainer from './AppContainer';

const App = () => {
  const initialState = {
    golds: 0
  }

  return (
    <AppContainer />
  );
}

export default App;